name: APP

on:
  pull_request:
    paths:
      - 'packages/app/**'
  push:
    paths:
      - 'packages/app/**'

jobs:
  lint:
    name: Lint
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - run: echo APP WORKFLOW
      - run: yarn
      - run: yarn lint

